[package]
name = "pico"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
bip39 = "2"
bitcoin = "0.32.5"
flutter_rust_bridge = "=2.10.0"
futures = "0.3"

iroh = { version = "=1.0.3", default-features = false, features = ["tls-ring"] }
iroh-mdns-address-lookup = "=0.4.0"
lightning-invoice = { version = "0.34", features = ["std"] }
picomint-base32 = { git = "https://github.com/joschisan/picomint", branch = "main" }
picomint-client = { git = "https://github.com/joschisan/picomint", branch = "main" }
picomint-core = { git = "https://github.com/joschisan/picomint", branch = "main" }
picomint-eventlog = { git = "https://github.com/joschisan/picomint", branch = "main" }
picomint-fountain = { git = "https://github.com/joschisan/picomint", branch = "main" }
picomint-lnurl = { git = "https://github.com/joschisan/picomint", branch = "main" }
picomint-sqlite = { git = "https://github.com/joschisan/picomint", branch = "main" }
regex = "1"
reqwest = { version = "=0.12.22", features = ["rustls-tls", "json"], default-features = false }
rustls = { version = "0.23", default-features = false, features = ["ring"] }
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["sync", "rt", "macros", "time"] }
url = "2"

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(frb_expand)'] }

[profile.dev.package]
secp256k1 = { opt-level = 3 }
secp256k1-sys = { opt-level = 3 }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = false
debug = "line-tables-only"

[profile.release.package]
secp256k1 = { opt-level = 3 }
secp256k1-sys = { opt-level = 3 }
